# Base de datos (obtener en neon.tech)
DATABASE_URL="postgresql://usuario:password@host/nombre_db?sslmode=require"

# Auth (generá un string random largo, ej: openssl rand -base64 32)
AUTH_SECRET="tu-secret-random-aqui"

# URL del sitio (local o producción)
NEXTAUTH_URL="http://localhost:3000"
# En producción: NEXTAUTH_URL="https://www.travelvyp.com.ar"
